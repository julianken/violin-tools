# GAPS.md

A ledger of repo / Claude-setup capabilities **deliberately not built yet**, each with the trigger that should wake it. This is a pre-code, solo, client-side, AI-agent-built repo, so most tooling other repos carry would have nothing to act on yet — no dependency graph to scan, no UI to guard, no deadline to age against. This file records *why* each thing is absent and *what* should bring it back, so a deferred item resurfaces when its trigger fires instead of being silently forgotten or re-litigated.

Scope: the repo's tooling, CI, agents, skills, and process scaffolding. Not a product backlog — features/tools live in issues and `DESIGN.md`, not here.

This file is itself drift-prone, so it sits in the `AGENTS.md` “Keeping docs and drift-prone files current” Update Triggers logic: when a process or roadmap change fires or retires one of the triggers below, reconcile this file in the same PR. A row whose trigger has already fired but is still parked under “Deferred” is a finding — raise it the way that section says (a non-blocking IMPORTANT note, never a merge blocker). Don't restate `AGENTS.md`, `DESIGN.md`, or `SECURITY.md` here; cross-reference them.

---

## Deferred (build when the trigger fires)

Sibling repos referenced: `detached-node` (the blog, `julianken/detached-node`) and `bird-sight-system` (local `bird-watch`) — the two repos this setup was mined from. Note that most of `bird-watch`'s `.claude/` is per-developer and gitignored (`.gitignore` ignores `.claude/*` except `.claude/skills/`), so where it's cited below as a model, it's local scaffolding to learn from, not a committed file to copy.

| Item | Trigger that should wake it | Why deferred / modeled on |
| --- | --- | --- |
| ~~Dependency hygiene: `knip` (dead-code/dep finder), grouped Dependabot config, lockfile sanity (`npm ci --dry-run`), `syncpack`~~ — **WOKEN (2026-06)** | ~~`package.json` + a build + source files land~~ — **fired** | **Trigger fired (S1, #36):** the foundation PR landed root + `apps/web` `package.json`s, a committed `pnpm-lock.yaml`, a real Vite build, and TS/TSX source — so there is now a dependency graph and lockfile to analyze. Only the trigger state is reconciled here; **building** these tools (`knip`, grouped Dependabot, lockfile-sanity check, `syncpack`) is a deferred follow-up, not part of S1. |
| ~~CI-gate `scripts/check-claude-shim.sh` as a GitHub Actions job~~ — **WOKEN (2026-06)** | ~~The first GitHub Actions workflow exists~~ — **fired** | **Resolved:** `.github/workflows/scaffolding.yml` (the repo's first workflow) runs `scripts/validate-scaffolding.sh` on every PR + push to `main`, which calls `check-claude-shim.sh` as its first invariant — so the shim guard is now CI-gated, not hand-run. Modeled on `detached-node`'s `meta-rule-consistency.yml`. |
| Soft-launch → dated-flip → promote-to-blocker rollout ritual + allowlist-with-dated-expiry hygiene — **IN USE (S8); capstone gates added S13, promotion-to-required still PENDING** | ~~A CI check arrives whose **failures are noisy or non-deterministic enough to need a staged rollout** (e.g. a linter or flaky e2e)~~ — **fired (S8)** | **Narrowed (2026-06):** the first non-shim deterministic check existed — `scripts/validate-scaffolding.sh` via `scaffolding.yml` — a trivially-deterministic presence gate with no false-positive surface, so it shipped as a hard gate with no ritual. **Re-confirmed (S1, #36):** the `gates` check (typecheck/lint/test/build) shipped as a second deterministic hard gate (Mergify `check-success = gates` + the branch ruleset, non-strict), again no ritual. **Trigger fired (S8, #45):** the Playwright motion **e2e** suite is the first noisy/non-deterministic check (a real browser can flake), so the ritual is **in use** — the `e2e` job ships **SOFT** (a separate `.github/workflows/ci.yml` job, NOT in the branch ruleset's required checks nor in `.mergify.yml` `check-success`); it runs on PR + push and uploads its report. **Extended (S13, #47):** the capstone added the **acceptance E2E + visual** specs (`select-root-scale`, `toggle-refs`, `palette-jump`, `visual.spec.ts`) into the SAME soft `e2e` job — so an `apps/web` E2E + visual CI gate now exists. **Promotion to a *required* check is still PENDING:** it is a branch-ruleset API change (not a file edit) the orchestrator performs separately, and over-gating a solo project is the owner's call — NOT done in #47. The `gates` job and its required status are unchanged. |
| Commit-gated `PreToolUse` hooks (block `console.log` / debug artifacts), scoped to **code only, never `.md`** — trigger **still OPEN** (lint gate is a backstop, not a replacement) | ~~There is shipping JS/TS~~ — **fired (S1, #36)**; build the hook to stop the write *before* it lands | **Trigger fired (S1, #36):** `apps/web` now ships real TS/TSX (`src/main.tsx`, `src/App.tsx`, tests), so there is code to lint-on-write. **Partial backstop (anti-slop lint PR, 2026-06):** `no-console: ['error', { allow: ['warn','error'] }]` (scoped to `apps/web/**`, off in tests/config) now catches a leftover `console.log` at the **CI/commit gate** — but a gate-time `error` is *not* the PreToolUse hook: the hook blocks the write *before* it lands (faster feedback, no failed commit), the lint rule only fails it after. So this row stays **OPEN** — the lint rule is a safety net, the PreToolUse hook (code-only, never `.md`) is still the deferred build. |
| `.claude/settings.json` (committed, minimal) | There's a hook or plugin worth sharing repo-wide | A committed settings file with nothing in it is clutter. The paired per-developer ignore (`.claude/settings.local.json`) is being added now, in this batch, since it can stand alone before any committed settings exist. Modeled on `bird-watch`'s per-developer `settings.local.json` (which is gitignored there, not committed). |
| More repo agents/skills + an `agents/_patterns` crosswalk + a scoped sub-agent mode (e.g. a Dependabot reviewer) | Those agents/skills actually exist | The design-reviewer is the *first* agent; a patterns crosswalk and a Dependabot mode are premature with one agent and no deps. Both modeled on `bird-watch`'s local `.claude` scaffolding (`agents/_patterns.md`, `agents/dependabot.md` — gitignored there). |
| Cursor committed config (`.cursor/rules/review-dispatch.mdc` pointer; optional `.cursor/mcp.json`, `.cursor/hooks.json`, `.cursor/commands`) + porting `design-reviewer` to Cursor | Cursor stops reading `AGENTS.md` / `.claude/*` natively (a version regression), **review dispatch parity gap confirmed** (a harness posts boilerplate `@julianken-bot` approval without the plan-review or PR-review skills), OR a built UI / MCP server / CI-style guard need arises | Issue #10 verified native `AGENTS.md` read — a fat duplicated rules tree is still drift. **Partial wake (2026-06):** thin `.cursor/rules/review-dispatch.mdc` landed as a pointer-only trigger for review dispatch; full `.cursor/` tree remains deferred. **CI-style-guard trigger fired (S10, #46):** the axe/Playwright a11y job (`.github/workflows/ci.yml` `a11y`) is a CI-style guard, so the row's "a built UI … / CI-style guard need arises" clause is now satisfied — recorded here per the plan-review obligation. This wakes the *question*, not the whole `.cursor/` tree: with the built UI now present (S6–S10) the Cursor design-screenshot / Playwright-MCP caveat below is the live follow-up, but building the fat `.cursor/` config is a separate decision deliberately NOT made in S10 (the thin pointer remains the only committed Cursor config). Future caveat: Cursor discovers `design-reviewer.md` but has no per-tool allowlist (degrades to coarse `readonly:true`) and Playwright MCP IDs resolve differently — revisit when the design screenshot pass matters. |
| Deeper per-harness adapter config beyond the thin pointers — Gemini settings (`.gemini/`, tool/MCP allowlists) and a Copilot-specific config surface | A harness stops honoring the prose pointer to `AGENTS.md`, OR a tool-specific capability (per-tool MCP/allowlist, hooks) is actually needed for that harness | The shipped adapters are pointer-only: `GEMINI.md` (`@AGENTS.md` import) and `.github/copilot-instructions.md` (prose pointer) point at `AGENTS.md`/`DESIGN.md`/`INSTANCE.md` without forking the rules — same import-don't-fork posture as the `CLAUDE.md` shim and the Cursor committed-config row above (`.cursor/rules/review-dispatch.mdc` pointer; full `.cursor/` tree deferred). A fatter per-tool config is drift until a harness needs it. |
| ESLint UI-lint plugins: **jsx-a11y** + **@eslint-react/eslint-plugin** (the non-hooks React rules — `no-array-index-key`, `no-unstable-context-value`, a11y `alt-text`/`aria-*`) | **S3 (first real UI)**; formalized as the lint half of the **S10 a11y step** | Deferred from the anti-slop lint PR (2026-06): `apps/web` is pre-UI, so react-hooks + react-refresh are the load-bearing minimum and there are no components/JSX for these to act on. `@eslint-react` is ESLint-10-native (replaces `eslint-plugin-react`, which has no ESLint-10 peer); **jsx-a11y peer caps at `^9`** (open ESLint-10 issue jsx-eslint/eslint-plugin-jsx-a11y#1075) — re-verify that peer/issue at adoption and pin/override if still unresolved. |
| ESLint curated quality subsets: **unicorn** (`prefer-node-protocol`, `no-useless-undefined`, `error-message`), **sonarjs** (`cognitive-complexity`, `no-identical-functions` — note **LGPL-3.0**, the only non-MIT entry), **regexp** (ReDoS / optimal quantifiers) | as each becomes relevant: unicorn/sonarjs when real domain code lands in `packages/theory` and the team wants modernization/complexity gating (enable **individual rules** `warn`-first, never `recommended` wholesale); regexp when meaningful regex appears (input parsing, ⌘K command-palette matching) | Deferred from the anti-slop lint PR (2026-06): full `recommended` presets are bikeshed churn / threshold-sensitive noise for an agent on a 3-file tree; the note-map pitch-classification (DESIGN §12.5) is legitimately branchy, so cognitive-complexity needs tuning, not a blanket import. |
| ESLint rule **`import-x/no-cycle`** (import-cycle detection) | **re-test on Node 22** (the intended runtime) — enable only if it actually fires | Held in the anti-slop lint PR (2026-06): non-functional on the Node 24 / import-x 4.16 validation toolchain (zero messages on a textbook 2-file cycle, resolver-independent). Leaving it `error` now would be a silent placeholder that catches nothing; the rest of import-x (`no-duplicates`, `order`) shipped. |
| Figma Code Connect + Figma→code token-sync automation | a shipped reusable component library lands AND an Org/Enterprise Figma plan with Dev/Full seats | Code Connect is blocked on Pro (`get_code_connect_map` needs a Developer seat in an Org/Enterprise plan) and there are no components to connect yet; `get_variable_defs` returns `{}` on the file, so Figma is not an MCP-readable token feed today — it is visual reference only. |
| A committed `.mcp.json` (root Figma/Claude MCP config) | CI or another non-interactive env needs deterministic, non-user-level Figma MCP access | The remote Figma server already resolves at the user/tool level via per-user OAuth, so a committed file carries no shared state — pure drift surface for zero gain today. |
| Automated **fill mode** for the template (placeholder-substitution skill or init CLI) | Standing up new products from `julianken/agentic-seed` becomes frequent enough that hand-editing `{{placeholders}}` is the bottleneck | The public empty template shipped as `julianken/agentic-seed` (a GitHub _template_ repo); today you fill its `{{placeholders}}` by hand. `project-bootstrap` documents `validate` vs `fill` modes but only `validate` is implemented — the automated fill path is the deferred remainder, not the template itself. (Was tracked as "bucket B"; the program doc that named it was retired in #34 — plans are no longer committed.) |
| **GCP load balancer + Cloud CDN + Google-managed end-to-end TLS** in front of the GCS bucket — deferred for cost (**v1 is SHIPPED & LIVE at https://strings-solo.com on the lean Cloudflare→GCS edge instead**, S13 #47 / S12 amendment #39, 2026-06-07) | **Traffic / latency metrics justify it** — i.e. the free Cloudflare edge stops being adequate (sustained traffic, a need for GCP-native CDN/cert/WAF, or measured edge latency that an LB would fix) | The lean architecture (public-read GCS bucket fronted by a free Cloudflare Worker for TLS + CDN + `www`→apex 301) is ~$0/mo and ample for a low-traffic static SPA; the standing LB (~$18/mo) is overkill at v1 volume. It is **additive later** in front of the same bucket (the bucket + WIF deploy don't change), so deferring it costs nothing now and the deploy IaC (`infra/`) already targets the bucket directly. Re-stood up via the originally-specified §4 LB/Cloud-CDN/managed-cert path if the trigger fires. See `DESIGN.md` §16 (hosting deferred list) and `INSTANCE.md` Status. |
| **Programmatic Figma design-lint** (automated contrast, token-coverage, touch-target checks directly against Figma frames) — blocked today by `get_variable_defs` returning `{}` + no MCP lint tool | Figma exposes a readable dev-mode status or token-feed (e.g. `get_variable_defs` returns real data OR a Figma lint MCP tool ships) | `get_variable_defs` returns `{}` on the violin-tools Figma file today, so Figma is visual reference only — not a machine-readable token source. A programmatic lint (contrast ratios, DESIGN.md §0 token alignment, touch-target minimums) requires either a `get_variable_defs` fix or a dedicated lint tool. Added as the pipeline system lands (P7 #113); retire this row when the Figma plan/tooling exposes a dev-status read. (See also `AGENTS.md` → Skill ownership → pipeline footnote + Figma Code Connect row above.) |
| **Consuming the `DEV_MODE_STATUS_UPDATE` webhook as a push signal** for the pipeline dashboard — deferred while the pipeline stays 127.0.0.1 gh-poll-only | Figma exposes a readable dev-mode status that the pipeline dashboard (`tools/pipeline-dashboard/`) can consume as a push event, OR a stable `DEV_MODE_STATUS_UPDATE` Figma webhook endpoint is available | The pipeline dashboard today polls `gh` for ground truth and uses in-process SSE for in-flight workflow events (both 127.0.0.1-bound). A push signal from Figma (e.g. design approval, frame published) would close the design-to-code gap without polling. Added as the pipeline system lands (P7 #113); retire if the Figma later dev-status read becomes available and the webhook is stable. |

